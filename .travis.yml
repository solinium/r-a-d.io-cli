dist: trusty
sudo: required

language: python
python:
  - "3.4"
  - "3.5"
  - "3.6"

os:
  - linux

notifications:
  email:
    recipients:
      - asg@airmail.cc
    on_success: never
    on_failure: never

install:
  - pip install -r requirements.txt

script:
  - sudo python3 base.py