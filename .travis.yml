dist: trusty
language: python
python:
  - "3.4"
  - "3.5"
  - "3.6"
os:
  - linux
env:
  - TARGET_ARCH=x64

notifications:
  email:
    recipients:
      - asg@airmail.cc
    on_success: never
    on_failure: never

install:
  - pip install -r requirements.txt

script:
  - python base.py